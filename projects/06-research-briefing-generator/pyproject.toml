[project]
name = "research-briefing-generator"
version = "0.1.0"
description = "Deterministic source-note ranking and Markdown briefing generation."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
research-briefing = "research_briefing.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
