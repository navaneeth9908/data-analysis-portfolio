[project]
name = "report-qa-agent"
version = "0.1.0"
description = "Offline report Q&A workflow with deterministic retrieval and citations"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
