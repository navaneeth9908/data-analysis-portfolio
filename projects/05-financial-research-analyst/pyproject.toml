[project]
name = "financial-research-analyst"
version = "0.1.0"
description = "Offline financial research metrics and Markdown brief generator for deterministic portfolio demos."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
financial-research = "financial_research.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
