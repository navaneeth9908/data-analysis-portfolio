[project]
name = "competitive-intelligence-pipeline"
version = "0.1.0"
description = "Offline competitive intelligence pipeline for public-source notes and competitor landscape summaries."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
competitive-intel = "competitive_intel.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
