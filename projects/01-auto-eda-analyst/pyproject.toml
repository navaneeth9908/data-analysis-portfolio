[project]
name = "auto-eda-analyst"
version = "0.1.0"
description = "Deterministic CSV profiling and Markdown report generation for a data-analysis portfolio."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
auto-eda = "auto_eda.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
